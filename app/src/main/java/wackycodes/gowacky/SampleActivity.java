package wackycodes.gowacky;

import android.content.Intent;
import android.os.Bundle;

import com.google.android.material.bottomnavigation.BottomNavigationView;

import androidx.appcompat.app.AppCompatActivity;
import androidx.navigation.NavController;
import androidx.navigation.Navigation;
import androidx.navigation.ui.AppBarConfiguration;
import androidx.navigation.ui.NavigationUI;

public class SampleActivity<subject> extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
//        setContentView(R.layout.activity_sample);
//        BottomNavigationView navView = findViewById(R.id.nav_view);
//        // Passing each menu ID as a set of Ids because each
//        // menu should be considered as top level destinations.
//        AppBarConfiguration appBarConfiguration =
//                new AppBarConfiguration.Builder(
//                R.id.w_BottomNavHomeButton, R.id.w_BottomNavShareButton, R.id.w_BottomNavNotificationButton).build();
//
//        NavController navController = Navigation.findNavController(this, R.id.nav_host_fragment);
//        NavigationUI.setupActionBarWithNavController(this, navController, appBarConfiguration);
//        NavigationUI.setupWithNavController(navView, navController);
    }

//    Share or invite Code...
//            Intent wShareIntent = new Intent(Intent.ACTION_SEND);
//            wShareIntent.setType("text/plan");
//            String body = "Wacky Sample message...!!", subject = "Wacky Sample Message!!";
//            wShareIntent.putExtra(Intent.SUBJECT, subject);
//            wShareIntent.putExtra(Intent.BODY, body);
//
//            startActivity(Intent.createChooser(inviteIntent, "invite by..."));

//    Share or invite Code...




}
